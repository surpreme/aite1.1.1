<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <androidx.cardview.widget.CardView
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        app:cardBackgroundColor="@color/primary_background_translate_inverse"
        app:cardCornerRadius="10dp"
        android:layout_marginBottom="@dimen/primary_row_spacing_general"
        app:layout_constraintLeft_toLeftOf="@id/button"
        app:layout_constraintRight_toRightOf="@id/button"
        app:layout_constraintBottom_toTopOf="@id/button">

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/content_list"
            android:layout_width="match_parent"
            android:layout_height="match_parent"/>

    </androidx.cardview.widget.CardView>

    <TextView
        android:id="@+id/button"
        style="@style/CustomRectangleActionButton"
        android:backgroundTint="@color/primary_background_shallow_color"
        android:layout_width="0dp"
        android:layout_height="55dp"
        android:text="@string/cancel"
        android:textColor="@color/review_second_user_color"
        android:layout_marginStart="@dimen/primary_row_spacing_middle"
        android:layout_marginEnd="@dimen/primary_row_spacing_middle"
        android:layout_marginBottom="@dimen/primary_row_spacing_middle"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"/>

</androidx.constraintlayout.widget.ConstraintLayout>